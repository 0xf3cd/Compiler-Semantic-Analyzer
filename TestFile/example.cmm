int x = .3;
int y = x * x;
int z = (x > y);
float w = x > y * (0.7 + b / (z != x)) - (x * x <= z);

float f1(int a) {
    int b = a * a;
    return b;
}

void f2() {
    return 108 * f1(18);
}

int main(void) {
    if(((f1(100) + f2(void) > 10.5)) != 100 + 25.9) {
        return x;
    } else {
        int q = 10 + w;
        return q;
    }
}
#